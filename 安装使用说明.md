# 选题过三关 · 安装与使用说明

这是一套帮你判断"一个选题值不值得做"的工具：图书、公众号图文、短视频、小红书笔记、播客音频都能用。核心逻辑是「选题过三关」——需求关、竞争关（硬门槛，卡住就不该推进）→ 回报关（加速器，决定要不要加码）。

你会收到三样东西，对应三种使用方式，按自己的情况选一种就行，不用三个都装：

| 文件 | 适合谁 |
|---|---|
| `topic-planner.skill` | 用 Claude Code / Cowork 的人 |
| `topic-planner-skill-package.zip` | 用豆包 / Kimi / 通义千问 / ChatGPT 等其他 AI 平台的人 |
| `离线工具/选题过三关-离线清单.html`（zip 包里也有） | 不想装任何东西、只想要一份能手动核对的清单 |

---

## 方式一：Claude Code / Cowork（最省事）

1. 拿到 `topic-planner.skill` 文件。
2. **Cowork**：直接点文件卡片上的「Save skill」按钮，装完即可用。
   **Claude Code**：把 `.skill` 文件重命名成 `.zip` 解压，整个文件夹放进项目的 `.claude/skills/` 目录下。
3. 之后正常聊天，说"帮我判断一下这个选题"、"这本书能不能做"之类的话就会自动触发，不用额外操作。

---

## 方式二：豆包 / Kimi / 通义千问 / ChatGPT 等平台

这些平台没有"技能"这个概念，但技能内容本身就是纯文字，可以直接当自定义指令用：

1. 解压 `topic-planner-skill-package.zip`。
2. 打开里面的 `agents/openai.yaml`，把 `system:` 后面的整段内容复制出来。
3. 粘贴进你用的平台的"自定义指令 / 人设 / System Prompt"设置里（一般在"创建智能体/Bot"或"设置"页面，不同平台叫法不同）。
4. **务必开启该平台的联网/搜索功能**——这套框架的判断必须有真实数据支撑，没联网就只能标"未核实"。
5. 直接把你的选题发给配置好的智能体，比如："帮我判断一下《XXX》这个短视频选题值不值得做"。

---

## 方式三：不用装任何东西，直接用离线清单（零门槛）

1. 找到 `离线工具/选题过三关-离线清单.html`，双击用浏览器打开——不需要联网，不需要安装任何东西。
2. 填好选题名称和媒介。
3. 点「① 生成检索提示词并复制」，把复制到的内容发给你手边**任意一个开着联网功能的 AI**（豆包、Kimi、千问、ChatGPT、Claude 都行，不需要装这个技能）。
4. 把 AI 返回的结果粘贴进「② 粘贴 AI 返回的 JSON 结果」框里，点「导入并自动计算」——7 项判断、依据、综合结论会自动填好。
5. 核对每一项依据是否站得住，觉得没问题就用「打印 / 导出 PDF」或「复制文字总结」留存结果。
6. 没有 AI 可用也没关系，7 项判断本身支持手动逐个填写。

这份清单所有内容只存在你自己电脑的浏览器里（localStorage），刷新不丢、换设备不同步，也不会上传到任何地方。

---

## 输出结果长什么样

无论用哪种方式，最后都会得到 7 项判断（成立 / 存疑 / 不成立，或强 / 中 / 弱）和一个综合结论：

- **推进**：需求关、竞争关全过。
- **补充材料后推进**：硬门槛里有 1 项存疑/不成立，但没触发降级阈值。
- **暂不推进**：硬门槛里 2 项及以上不成立，或"刚需强度""即时驱动力"这两项里有任意一项单独不成立。

回报关（触达潜力、复利潜力）不影响推不推进，只影响要不要加码投入。

---

## 常见问题

**我的 AI 平台没有联网/搜索功能怎么办？**
仍然可以用，但结果里每一项要明确标"未检索，基于经验判断"，综合结论也要提醒"未经数据核验，仅供参考"——不能装作查过数据。

**选题不属于这五类媒介怎么办？**
说清楚具体是什么（知乎专栏、直播、B站长视频等），会自动类推到最接近的一类，并说明类推依据。

**能不能同时判断"发公众号"还是"发小红书"哪个更合适？**
可以，说清楚是多媒介同步还是还没想好，会分别给判断或者先给一个"哪个更合适"的建议。

**这套框架适用于海外市场选题吗？**
框架本身和市场无关，把数据源换成对应地区平台就行（比如图书查 Amazon/Goodreads，短视频查 TikTok/YouTube），三关结构和判断阈值不变。

---

有问题或想反馈可以找豆酱。
